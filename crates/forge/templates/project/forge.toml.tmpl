# FORGE Configuration
# See https://forge.dev/docs/configuration for all options

[project]
name = "{{name}}"

[database]
url = "${DATABASE_URL}"

[gateway]
port = 8080

[observability]
enabled = true
