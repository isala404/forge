# forgelib for Dart

This is the official Dart package name for Forge, the standard library for
agent-built SaaS. This metadata release exists so the Forge project owns its
first-party Dart package coordinate while the full Dart binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
