# forgelib for Java and Kotlin

This is the official Maven coordinate for Forge:

```text
io.github.isala404:forgelib
```

This metadata release exists so the Forge project owns its first-party JVM
package coordinate while the full Java/Kotlin binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
