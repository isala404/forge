# forgelib for Go

This is the official Go module path for future Forge support:

```text
github.com/isala404/forge/bindings/go
```

Go packages are discovered from module paths rather than reserved in a central
registry, so this scaffold establishes the module path inside the Forge repo.

Use the Rust, Node.js, or Python packages for production code today.
