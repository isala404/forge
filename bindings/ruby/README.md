# forgelib for Ruby

This is the official Ruby gem name for Forge, the standard library for
agent-built SaaS. This metadata release exists so the Forge project owns its
first-party Ruby package coordinate while the full Ruby binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
