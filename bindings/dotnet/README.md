# ForgeLib for .NET

This is the official NuGet package name for Forge, the standard library for
agent-built SaaS. This metadata release exists so the Forge project owns its
first-party .NET package coordinate while the full .NET binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
