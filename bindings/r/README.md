# forgelib for R

This is the official R package name for Forge, the standard library for agent-built SaaS. This metadata release exists so the Forge project owns its first-party R package coordinate while the full R binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
