# forgelib for Elixir

This is the official Hex package name for Forge, the standard library for
agent-built SaaS. This metadata release exists so the Forge project owns its
first-party Elixir package coordinate while the full Elixir binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
