# forgelib for Haskell

This is the official Hackage package name for Forge, the standard library for
agent-built SaaS. This metadata release exists so the Forge project owns its
first-party Haskell package coordinate while the full Haskell binding is
prepared.

Use the Rust, Node.js, or Python packages for production code today.
