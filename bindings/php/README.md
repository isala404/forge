# forgelib for PHP

This is the official Composer package name for Forge:

```text
isala404/forgelib
```

This metadata release exists so the Forge project owns its first-party PHP
package coordinate while the full PHP binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
