# ForgeLib for Swift

This is the official Swift package and CocoaPods pod name for Forge. This
metadata release exists so the Forge project owns its first-party Apple package
coordinate while the full Swift binding is prepared.

Use the Rust, Node.js, or Python packages for production code today.
