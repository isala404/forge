# @isala404/forgelib for JSR

This is the official JSR package name for Forge, the standard library for
agent-built SaaS. This metadata release exists so the Forge project owns its
first-party JSR package coordinate while the full TypeScript runtime package is
prepared.

Use the npm package for Node.js production code today.
