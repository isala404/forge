[package]
name = "codegen"
version = "0.0.0"
edition = "2021"
publish = false
description = "Generates Forge's cross-language binding DTO definitions from one schema (tools/codegen/src/schema.rs)."

[[bin]]
name = "codegen"
path = "src/main.rs"

# Standalone tool: its own workspace root so it never entangles the bindings' builds.
[workspace]
